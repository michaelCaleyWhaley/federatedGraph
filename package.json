{
  "name": "apollofederatedgraph",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "./router --supergraph=./schemas/supergraph.graphql",
    "dev": "./router --supergraph=./schemas/supergraph.graphql --dev",
    "compose": "rover supergraph compose --config ./config/supergraph-config.yaml > ./schemas/supergraph.graphql",
    "test": "echo \"Error: no test specified\" && exit 1",
    "rover": "rover"
  },
  "author": "Michael Caley",
  "license": "ISC",
  "dependencies": {
    "@apollo/rover": "^0.14.1"
  }
}
